#include <iostream.h>
using namespace std;

int main(int argc, char const *argv[])
{
	//priority_queue<int> qu;// made automatic max heap 
	// this is for min heap 
	priority_queue<int, vector<int >, greater<int> >qu;
	int h;
	cin>>h;
	while(eof)
	{
		if (!h=-1)
		{
			int k, l, m;
			qu.push_back(k);
			qu.push_back(l);
			qu.push_back(m);
			x=min(k,(min(l,m)))
			if (/* condition */)
			{
				/* code */
			}

		}	}


	return 0;
}